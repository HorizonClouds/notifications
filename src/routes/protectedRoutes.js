//import express from 'express';
//import { authorize } from '../middlewares/authMiddleware.js';  // Importamos el middleware de autenticación

//const router = express.Router();

//router.get('/v1/notifications/profile', authenticate(), (req, res) => {
//  res.json({ message: 'Welcome to your profile', user: req.user });
//});

//router.get('/v1/notifications/admin-only', authenticate('admin'), (req, res) => {
//  res.json({ message: 'Welcome, admin!' });
//});


//export default router;
